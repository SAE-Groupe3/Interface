<Directory "/var/www/html">
    AllowOverride All
    Require all granted
    EnableSendfile Off
</Directory>

# Désactive l'opcache PHP pour éviter la mise en cache des fichiers
php_flag opcache.enable Off
